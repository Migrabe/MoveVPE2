name: movevpe2

services:
  - name: web
    type: web
    docker:
      dockerfile: Dockerfile.koyeb
    ports:
      - port: 3000
        protocol: http
    routes:
      - path: /
        port: 3000
    env:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
